name: Check Open Automated PRs
on:
  schedule:
    # runs on weekdays at 5PM UTC
    - cron: '46 17 * * 1-5'

permissions:
  contents: write
  id-token: write

jobs:
  check-prs:
    secrets: inherit
    uses: Workiva/gha-automated-pr-check/.github/workflows/automated-pr-check.yaml@v0.1.2
    with:
      slack-alert-channel: 'C095QEHMWES' #grc-bot-prs-graph
